/**
 * 邮箱地址
 */
const email = 'zhouzefu@126.com'; 

const status = {
    
};

/**
 * 项目地址
 */
const projects = [{
    id: 1,
    title: '个人博客',
    remark: '我的个人博客技术文章等',
    // status:
}]

/**
 * 页面加载时
 * @param {Event} res 
 * @returns 
 */
window.onload = res => initPageData(res);

/**
 * 初始化页面数据
 * @param {Event} res 
 */
function initPageData(res) { }